import { ValidaCalificacionPipe } from './valida-calificacion.pipe';

describe('ValidaCalificacionPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidaCalificacionPipe();
    expect(pipe).toBeTruthy();
  });
});
